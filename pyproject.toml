[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "arena-mcp"
version = "0.1.0"
description = "MCP server for Arena.ai leaderboard tools"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]